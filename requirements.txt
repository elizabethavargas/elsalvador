# Core scraping
requests>=2.28.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data handling
pandas>=2.0.0

# Date parsing
python-dateutil>=2.8.0

# Optional: Cloudflare bypass (significantly reduces 403s on elsalvador.com)
# pip install cloudscraper

# ── Topic modeling ────────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
# umap-learn and hdbscan (standalone) both have native lib issues on Python 3.8;
# topic_modeling.py uses sklearn PCA + sklearn.cluster.HDBSCAN instead
scikit-learn>=1.3.0
plotly>=5.20.0
